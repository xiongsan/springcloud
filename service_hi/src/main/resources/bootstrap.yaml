spring:
  application:
    name: service-hi # 服务名称
  profiles:
    active: dev #开发环境，这里是dev
  cloud:
    nacos:
      username: nacos
      password: nacos
      config:
          server-addr: localhost:8848
          file-extension: yaml # 文件后缀名